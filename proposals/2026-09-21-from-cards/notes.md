# 归并说明

- 输入：`input-cards/` 中 15 张 `migloop-case/2` 情景卡；从空库初始化，没有读取或合并任何既有经验库。
- 发布：12 条经验全部完成来源支持、适用边界和跨项目迁移性审核，状态均为 `active`；本版没有更新、退役、争议或待复查条目。
- 同根合并：
  - 两张 Scaffold / 状态栏卡合并为“沿父子布局链只承接一次，并覆盖同一内容区所有层”的安全区责任经验。
  - 单行 lineHeight 与多行 lineSpacing 两张卡合并为“分别建模自然行盒与多行行间距”的文本度量经验。
  - TopAppBar 单侧 inset 与 IconButton 隐式最小尺寸两张卡合并为“区分调用方修饰符与 Material 库内测量及槽位”的组件经验。
- 保持拆分：同批混合模式卡分别形成“共同离屏组”和“把源颜色运算预混进色标”两条经验，因为前者解决参与混合的节点结构，后者解决算子替换后的颜色数学，触发条件和检查方法不同。
- 其余卡按独立机制保留：整数几何单位域、声明式布局实际测量、ArkUI Builder 槽、阴影单位与共享通路、描边测量副作用、多行省略语义、导航返回时的框架状态恢复。
- 提炼边界：项目名、文件名、任务号、历史修复值和证据树只留在卡片；文档未明确或设备行为未验证的条件写入经验的 `unless` 或验证动作，没有扩成无条件规则。

发布 revision：`3c8d212f557f1ef86369c96f5eed1d4074909db9722671cf5489f3e10e529a41`
