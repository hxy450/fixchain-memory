# 局部细化与可选检查

本轮由 Codex 基于已有 12 条经验及来源卡做局部修订，未重新制卡、未调用独立模型重新归并、未运行新迁移。

- 基础版本：`3c8d212f557f1ef86369c96f5eed1d4074909db9722671cf5489f3e10e529a41`
- 新版本：`410b563c37b78acf42584c1a0179e96bd8918762ce4b696c401bb026f142fc05`
- 发布器：`0.7.2`。阅读入口仍为仓库的 `memory/index.md`。

## 变更

1. 召回的默认动作变为“理解条件 → 采用做法 → 继续任务”。检查只在条件疑问、输入冲突或关键假设需要核实时按需执行，优先复用已有证据和正常测试；项目原有必需测试不受影响。
2. Builder、整数单位和导航状态补最小结构/事件示例，不要求存在历史项目的函数、组件或参数名。Builder 与单侧 inset 条目不填检查项，其余检查写明使用条件；原有默认扫描、额外截图与测试矩阵不再放进 how。
3. 原 Material 综合经验保留身份 `lesson-b7e0ff573247b0324bef`，收窄到声明尺寸/布局/触控区域；新增 `lesson-de5e7c73d852009e81b6` 单独说明单侧 inset 与槽位。来源按对应卡拆开，未重复扩大支持范围。
4. 安全区按具体边界分配责任，分别表达内容与遮罩范围；颜色混合区分画布范围与算子等价，遮罩替代有明确前提。其他条目只调整当前动作与可选检查的边界。

全部 43 个 case/claim/revision 绑定来自旧版，覆盖同一批 15 张卡。旧 12 个 lesson ID 保留，新拆分一项；卡片内容、版本和核验状态未修改，旧快照保留。

## 验证

- 使用维护 skill 的 `apply` 与 `export --link-cards`，没有手改阅读正文或内部快照。
- 13 条 active、19 个主题、33 个 Markdown 文件；全部阅读文件 SHA256 与 manifest 一致，79 个相对链接可解析。
- 43 个来源绑定可定位到对应卡的 claim 与确切 revision；来源集合与修订前一致。
- 脚本回归：135 passed / 1 skipped；缺少 check 的示例可完整发布，空检查节不输出，旧检查仍可读取。
- 这些校验说明结构与来源引用完整，不证明经验在新工程中的召回率、耗时或迁移收益。下一次迁移需固定本版本后观察实际采用情况。
